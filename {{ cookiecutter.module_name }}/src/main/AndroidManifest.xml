<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="{{ cookiecutter.package_name }}">
    <application>
        <provider
            android:authorities="${applicationId}.{{ cookiecutter.module_name }}-module"
            android:name=".Module"
            />
    </application>
</manifest>
